import std;

module std::container {
	interface ILinearCollection<type T> {
		@NoDiscard
		fn contains(T item) -> bool;
		
		@NoDiscard
		fn getSize() -> Index;
		
		@NoDiscard
		fn isEmpty() -> bool;
	}
}