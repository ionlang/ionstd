import std;
import std::container;

module std::container {
	struct Array<type TData, i32 TLength>
		extends Wrapper<TData[TLength]>, LinearCollection<TData>, Iterator<TData> {
		
		constructor() :
			Wrapper<TData[TLength]>(),
			LinearCollection<TData>(),
			Iterator<TData>() { }
		
		operator [](Index index) {
			# TODO
		}
		
		# TODO: Methods, and override parent 'Iterator'.
	}
}